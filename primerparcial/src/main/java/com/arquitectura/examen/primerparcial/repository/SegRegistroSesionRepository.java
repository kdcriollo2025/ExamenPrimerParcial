package com.arquitectura.examen.primerparcial.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.arquitectura.examen.primerparcial.model.SegRegistroSesion;

public interface SegRegistroSesionRepository extends JpaRepository<SegRegistroSesion, String> {

}
